# Local OS / editor cruft
.DS_Store
*.swp
*.swo
*~

# Generated one-off AI notes / exports
gemini-code-*.md

# Local overrides for config.env — this is where REAL SECRETS live
# (auth keys, local paths, resource overrides, etc.). Launch and build scripts
# source config.env first and then config.env.local if it exists.
config.env.local

# Validator scratch
/tmp/
